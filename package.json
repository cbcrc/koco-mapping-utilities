{
  "name": "koco-mapping-utilities",
  "version": "1.0.0",
  "authors": [
    "David D'Amico <david.damico@cbc.ca>"
  ],
  "description": "Knockout mapping helper utilities.",
  "main": "src/koco-mapping-utilities.js",
  "keywords": [
    "koco",
    "knockout",
    "utilities",
    "helpers"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "knockout": "^3.3.0"
  }
}
